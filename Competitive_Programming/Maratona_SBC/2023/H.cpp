#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll n, s;
    while(n--) {
        ll li, ri, ci;
        cin >> li >> ri >> ci;
    }

    return 0;
}