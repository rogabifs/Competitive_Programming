#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll n, k;
    vector<ll> v;
    cin >> n >> k;
    while(n--) {
        ll u, i = 0;
        cin >> u;
        cout << u;
        v.push    
    }


     
    return 0;  
}